# TalentPool

Eightfold smartapply entity. Adding the candidate to the Talent Pool

## Operations

| Op | Method | Path | Caller ID |
|----|--------|------|-----------|
| create | POST | `/talent-pool` | `talent-pool-create` |

## Hooks

- `useCreateTalentPool()` — mutation (op: `create`)

## Services (non-React contexts)

- `createTalentPool` — op: `create`

## Example

```ts
// No standard ops on this entity.
```
